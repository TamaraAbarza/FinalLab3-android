<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    android:backgroundTint="@android:color/white"
    app:cardCornerRadius="12dp"
    app:cardElevation="3dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <!-- Tipo de certificado -->
        <TextView
            android:id="@+id/tvTipoCertificado"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Certificado de Participación"
            android:textColor="#A52A2A"
            android:textSize="13sp"
            android:fontFamily="sans-serif-medium"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/imgCertificado"
            app:layout_constraintHorizontal_bias="0" />

        <!-- Título del certificado -->
        <TextView
            android:id="@+id/tvTituloCertificado"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Tech Innovators Summit 2024"
            android:textColor="#000000"
            android:textSize="15sp"
            android:fontFamily="sans-serif-medium"
            app:layout_constraintTop_toBottomOf="@id/tvTipoCertificado"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/imgCertificado" />

        <!-- Fecha -->
        <TextView
            android:id="@+id/tvFechaCertificado"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="July 15, 2024"
            android:textColor="#A52A2A"
            android:textSize="13sp"
            app:layout_constraintTop_toBottomOf="@id/tvTituloCertificado"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Botón descargar -->
        <Button
            android:id="@+id/btnDescargar"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:layout_marginTop="8dp"
            android:backgroundTint="#F3EDED"
            android:text="Descargar"
            android:textAllCaps="false"
            android:textColor="#000000"
            android:textSize="14sp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvFechaCertificado" />

        <!-- Icono de PDF -->
        <ImageView
            android:id="@+id/imgCertificado"
            android:layout_width="70dp"
            android:layout_height="90dp"
            android:src="@drawable/ic_pdf"
            android:scaleType="centerCrop"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>
